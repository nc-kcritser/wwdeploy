# Install minimal OS (Rocky 9.3) on head node
	Configure at least one ip address, so that you can connect to it once the installation completes.

# Upload files to the head node.
	configure_master_host.sh
	openhpc-set-vars.sh
	
	Rocky-9.3-x86_64-dvd.iso
	OM-SrvAdmin-Dell-Web-LX-11.0.1.0-5494.RHEL9.x86_64_A00.tar.gz
	MLNX_OFED_LINUX-23.10-3.2.2.0-rhel9.3-x86_64.tgz
	dell_bright_9x_install-1.2.8.tgz
	hpcx-v2.18.1-gcc-mlnx_ofed-redhat9-cuda12-x86_64.tbz
	l_BaseKit_p_2024.0.1.46_offline.sh
	l_HPCKit_p_2024.0.1.38_offline.sh
		
	
	# Run the configure script
	./configure_master_host.sh
		Disable firewalld/SELinux
		Configure hostname
		Configure DNS / name servers
		Configure provisioning ethernet interface
		Configure alias interface for ipmi/mgmt connectivty
		Configure external ethernet interface
		Configure time zone
		Configure time server
		Configure local OS repo
		Install prerequisite RPM packages	
		Install Dell OpenManage
		Install Mellanox OFED
		Configure iDRAC/ipmi interface (requires OMSA to be installed)
		Configure InfiniBand interface (If InfiniBand is part of solution)
		Update master node firmware
		Update master node CPLD firmware
		Install/Configure Warewulf v4
			Install
			Configure
			Default
			Containers
			Profiles
			Networks
		Install OpenHPC v3.x
		Configure File Synchronization
		Install Mellanox HPC-X
		Install Intel oneAPI
		Create Users/Groups
		Configure User tests
		Configure Slurm Workload Manager
		Enable firewalld
		Secure sshd with Fail2Ban
		Configure Caching Nameserver
