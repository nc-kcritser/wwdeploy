## Contains Template Files for use in scripting 

Current list 
- host-etc-hosts.ww         # changes the hostfile entries to mimic the BCM Style (.ib.cluster, .eth.cluster, .idrac.cluster)
                            # This is for the headnode(s)
- hosts-etc-hosts.ww        # changes the hostfile entries to mimic the BCM Style (.ib.cluster, .eth.cluster, .idrac.cluster)
                            # This is the version for the compute/gpu/login nodes